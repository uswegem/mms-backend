import { Injectable } from '@nestjs/common';

/**
 * Refresh token rotation — implement in identity module phase.
 */
@Injectable()
export class RefreshTokenService {
  // TODO: issue, rotate, revoke refresh tokens
}
